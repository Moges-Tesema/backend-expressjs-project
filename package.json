{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend practive with traversy media youtube video",
  "main": "server.js",
  "type":"module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "dev": "node --watch --env-file=.env server"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.1"
  }
}
